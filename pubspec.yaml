name: solpaws
description: blockchain game to speedrun event.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  solana: ^0.29.1
  flutter_secure_storage: ^8.0.0
  solana_web3: ^0.0.8
  borsh: ^0.3.1+2
  borsh_annotation: ^0.3.1+3
  bdk_flutter: ^0.28.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/w.png
